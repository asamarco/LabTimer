<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/widgetBackground"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/widget_margin">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal">

        <Chronometer
            android:id="@+id/widgetTimerText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textAlignment="center"
            android:textColor="@android:color/white"
            android:textSize="28sp"
            android:textStyle="bold"
            android:typeface="monospace"
            tools:text="512521" />


    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView style="@style/WidgetButtonHorizontalSpace" />

        <Button
            android:id="@+id/widget_minute_button"
            style="@style/widgetButtons"
            android:text="@string/minute" />

        <TextView style="@style/WidgetButtonHorizontalSpace" />

        <Button
            android:id="@+id/widget_second_button"
            style="@style/widgetButtons"
            android:text="@string/second" />

        <TextView style="@style/WidgetButtonHorizontalSpace" />

        <Button
            android:id="@+id/widget_start_button"
            style="@style/widgetButtons"
            android:layout_width="wrap_content"
            android:text="@string/start" />

        <TextView style="@style/WidgetButtonHorizontalSpace" />

    </LinearLayout>

</LinearLayout>